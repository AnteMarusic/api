#include <stdio.h>
#include "maxHeap.h"

/*
 int main() {
    struct Heap heap = max_heap_create_stack();
    int i = 0;
    for (i = 0; i < 10; i++) {
        max_heap_insert(&heap, i);
    }
    max_heap_print_tree(&heap);
    printf("\n");
    max_heap_remove(&heap, 0);
    max_heap_print_tree(&heap);
    printf("\n");
    max_heap_remove(&heap, 9);
    max_heap_print_tree(&heap);
    printf("\n");
    return 0;
}
 */