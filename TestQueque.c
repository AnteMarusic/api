#include "queque.h"
#include <stdio.h>

/*
int main () {
    int i;
    struct Queue queue = queue_create ();
    queue_print(&queue);
    for (i = 0; i < 10; i ++) {
        enqueue(&queue, i);
        queue_print(&queue);
    }
    for (i = 0; i < 10; i ++) {
        dequeue(&queue);
        queue_print(&queue);
    }
}
 */
