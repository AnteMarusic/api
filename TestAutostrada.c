#include "autostrada.h"
#include <stdio.h>

/*
int main () {
    enum boolean bool;
    struct Autostrada a = autostrada_create();
    int i;
    for (i = 0; i < 10; i++) {
        bool = stazione_create_and_insert(&a, i*2);
        printf ("insert %d\n", bool);
    }
    bool = stazione_create_and_insert(&a, 5);
    printf ("insert %d\n", bool);
    bool = autostrada_stazione_create_and_insert(&a, 6);
    printf ("insert %d\n", bool);
    autostrada_print(&a);
    max_heap_insert (&a.stazioni[0].heap, 10);
    bool = stazione_delete(&a, 120);
    printf ("delete %d\n", bool);
    bool = stazione_delete(&a, 6);
    printf ("delete %d\n", bool);
    bool = autostrada_stazione_elimina_stazione(&a, 6);
    printf ("delete %d\n", bool);
    autostrada_print(&a);
}
 */